{{#if hasBlock}}
  {{yield}}
{{else}}
  <ListenButton @class="stream-banner-listenbutton nudge" @type="blue-boss" @itemPK={{activeStream.slug}} @itemTitle={{activeStream.name}}>
    Listen Live
  </ListenButton>
{{/if}}
